<%= form_tag doctors_path, method: :get do %>
  <%= text_field_tag :speciality,
    params[:speciality],
    class: "form-control",
    placeholder: "La spécialité de mon praticien.ne"
  %>

  <%= text_field_tag :gender,
    params[:gender],
    class: "form-control",
    placeholder: "Le genre de mon praticien.ne"
  %>

   <%= text_field_tag :profession,
    params[:profession],
    class: "form-control",
    placeholder: "La profession de mon praticien.ne"
  %>

 <%= text_field_tag :convention,
    params[:convention],
    class: "form-control",
    placeholder: "La convention de mon praticien.ne"
  %>

  <%#= select_tag "tag", options_from_collection_for_select(Tag.all, "id", "name") %>
  <%#= select_tag "badge", options_from_collection_for_select(Badge.all, "id", "picto") %>

  <%= submit_tag "Search", class: "btn btn-primary" %>
<% end %>
