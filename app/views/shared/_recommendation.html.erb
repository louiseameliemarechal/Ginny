<div class="modal fade" id="recommendationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" data-controller="modal">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <h4 class="modal-title text-align-center" id="recommendationModal">Ajouter une recommandation</h4>
      </div>
      <div class="modal-body px-4">
        <p data-modal-target="message">Merci de n’ajouter une recommandation de soignant.e que si tu l’as toi-même consulté.e et que tu souhaites le recommander à tes amis.
          <br>Autrement, tu peux le mettre en favoris.
        </p>

        <div class="d-none" data-modal-target="searchForm">
          <%= simple_form_for :search, url: doctors_path, method: :get, data: { controller: "search-form" } do |f| %>

            <div class="row d-flex justify-content-center">
              <div class="col align-self-enter d-flex">
                <%= f.input :name_or_specialty,
                  input_html: { value:  params.dig(:search, :name_or_specialty) },
                  class: "form-control",
                  placeholder: "Nom du praticien.ne ou spécialité",
                  label: "<i class='fas fa-search text-primary pin'></i>".html_safe,
                  required: false
                %>
              </div>
            </div>

            <div class="pt-4 mt-4 d-flex justify-content-center">
              <%= f.submit :Chercher, class: "button-main mb-4" %>
            </div>

          <% end %>
        </div>

      </div>
      <div class="modal-footer p-4" data-modal-target="footer">
        <div class="d-flex align-items-center">
          <input class="p-3" type="checkbox" id="opt-in" data-modal-target="checkbox">
          <label class="p-3" for="opt-in" data-modal-target="label">Oui, je recommande un.e soignant.e que j'ai consulté.e</label>
        </div>
        <div class="button-main" data-action="click->modal#change">Suivant</div>
        <div class="d-none">
          <%= link_to 'Suivant', search_path, data: { modal_target: 'nextButton' } %>
        </div>
      </div>
    </div>
  </div>
</div>
