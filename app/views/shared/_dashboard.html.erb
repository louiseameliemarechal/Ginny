<div id='dashboard' class='hidden'>
  <% if user_signed_in? %>
    <% if current_user.photo.attached?%>
      <%= cl_image_tag current_user.photo.key, class: "avatar-xxl mb-3", width: 200 %>
    <% end %>
    <h1><%= current_user.username %></h1>
    <p><%= pluralize(current_user.friendships.count, 'ami', 'amis')%></p>
    <%# <%= current_user.recommendations.count%>
    <p>x recommandations</p>



    <h3>Soignants sauvgardés</h3>
    <% current_user.favorites.includes(:doctor).each do |favorite| %>
      <ul>
        <li><%= "#{favorite.doctor.first_name} #{favorite.doctor.last_name} - #{favorite.doctor.profession}"%></li>
        <li><%= link_to 'remove', favorite_path(favorite), method: :delete %></li>
      </ul>
    <% end %>
    <hr>
    <%# <p><%= current_user.doctors</p> formule ok quand on aura fav doctors%>

    <%= link_to 'Edit', edit_user_registration_path%><br>
    <%= link_to "Log out", destroy_user_session_path, method: :delete %>
  <% else %>
    <%= link_to "Login", new_user_session_path, class: "nav-link" %>
  <% end %>
</div>
